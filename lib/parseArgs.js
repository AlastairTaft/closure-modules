/**
 * @fileoverview Parse the arguments, this is intended to return an object
 * that's in the same format that could be specified in the package.json
 */

function parseArgs(args){
  //return process.argv.slice(2)
  throw ("Command line arguments not yet supported, please configure the " + 
    "package.json instead")
}

module.exports = parseArgs